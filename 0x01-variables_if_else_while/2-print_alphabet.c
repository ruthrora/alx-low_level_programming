#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase.
 *
 * Return: Always 0.
 */
int main(void)
{
	char b;

	for (b = 'a'; b <= 'z'; b++)
	       putchar(b);

putchar('\n');

return (0);
}
