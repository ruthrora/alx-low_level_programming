#include "main.h"

/**
 * _isdigit -  function that checks for a digit (0 through 9).
 * @a: int to be checked
 * Return: 1 if a is a digit, 0 else
 */
int _isdigit(int a)
{
	if (c >= 48 && c <= 57)
		return (1);
	return (0);
}
