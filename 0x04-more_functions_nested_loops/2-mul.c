#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: a -first variable
 * @b: b -first variable
 * Return: Always 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b;

	return (mul);
}
