#include "main.h"
#include <stdio.h>

/**
 * wildcmp - compares two strings and returns
 * @s1: first input
 * @s2: second input
 *
 * Return: Always 0
 */
int wildcmp(char *s1, char *s2)
{
	return (0);
}
